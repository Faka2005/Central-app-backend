generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  username  String
  email     String   @unique
  password  String
  role      String   @default("user")
  createdAt DateTime @default(now())

  profil Profil? @relation("UserProfil")

  sentFriendRequests     Friend[] @relation("UserFriends")
  receivedFriendRequests Friend[] @relation("UserFriendOf")

  sentMessages     Message[]  @relation("SentMessages")
  receivedMessages Message[]  @relation("ReceivedMessages")

  passwords        Password[] @relation("UserPasswords")
}

model Profil {
  id        String     @id @default(uuid())
  firstname String
  lastname  String
  bio       String?
  interests Interest[]
  niveau    String
  campus    String

  userId String? @unique
  user   User?   @relation("UserProfil", fields: [userId], references: [id])


}

model Interest {
  id      String   @id @default(uuid())
  name    String   @unique
  profils Profil[]
}

model Lesson {
  id           String   @id @default(uuid())
  title        String
  content      String   // L'énoncé (Markdown supporté)
  niveau       String
  language     String   // ex: "javascript"
  
  starterCode  String   // Le code affiché par défaut dans Monaco
  solution     String?  // Le résultat attendu (ex: "10")
  validation   String?  // Une Regex stockée sous forme de String
  
  createdAt    DateTime @default(now())
}

model Friend {
  id        String   @id @default(uuid())
  userId    String
  friendId  String
  status    String   @default("pending")
  createdAt DateTime @default(now())

  user   User @relation("UserFriends", fields: [userId], references: [id])
  friend User @relation("UserFriendOf", fields: [friendId], references: [id])

  @@unique([userId, friendId])
}

model Reciter {
  id        String   @id @default(uuid())
  name      String
  slug      String   @unique
  basePath  String
  createdAt DateTime @default(now())

  surahs Surah[]
}

model Surah {
  id        String   @id @default(uuid())
  number    Int
  name      String
  audioFile String
  createdAt DateTime @default(now())

  reciterId String
  reciter   Reciter @relation(fields: [reciterId], references: [id])

  @@unique([number, reciterId])
}

model Service {
  id          Int      @id @default(autoincrement())
  name        String   @unique
  description String
  link        String
  etat        Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Password {
  id          String   @id @default(uuid())
  description String
  site        String
  email       String
  password    String
  createdAt   DateTime @default(now())

  userId String
  user   User   @relation("UserPasswords", fields: [userId], references: [id])
}

model Message {
  id         String   @id @default(uuid())
  senderId   String
  receiverId String
  content    String
  isRead     Boolean  @default(false)
  createdAt  DateTime @default(now())

  sender   User @relation("SentMessages", fields: [senderId], references: [id])
  receiver User @relation("ReceivedMessages", fields: [receiverId], references: [id])
}
