//import { userRepository } from "../repository/user.repository";
//
//export const userService = {
//  getUserById: (id: string) => userRepository.findById(id),
//  getAllUsers: () => userRepository.findAll(),
//  deleteUser: (id: string) => userRepository.delete(id),
//  updateRole: (id: string, role: string) => userRepository.updateRole(id, role),
//  resetPassword: (id: string, newPassword: string) => userRepository.resetPassword(id, newPassword),
//};